server {
    listen 80;
    server_name _;
    root /usr/share/nginx/html;
    index index.html;

    # All compute runs client-side (wasm) - no backend to proxy to.
    types {
        application/wasm wasm;
    }

    location / {
        try_files $uri $uri/ /index.html;
    }
}
